import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.circleci.fastlane',
  appName: 'circleci-ci-cd',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
