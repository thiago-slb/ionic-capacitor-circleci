import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'circleci-ci-cd',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
